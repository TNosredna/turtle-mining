print("Slot1:Rails, Slot2:Power Rails")
print("Slot3:Redstone Torches, Slot4:Torches")
print("Slot5:Cobblestone")
write("How long should the tunnel be?:")
length = io.read()
length1 = length + 0
power = 0
while length1 > 0 do
    laeng1 = (length1 - 1)
    while turtle.detect() == true do
        turtle.dig()
        sleep(0.5)
    end
    turtle.forward()
    turtle.select(5)
    turtle.placeDown()
    while turtle.detectUp() == true do
        turtle.digUp()
        sleep(0.5)
    end
    turtle.up()
    turtle.select(5)
    turtle.placeUp()
    turtle.down()
    turtle.turnLeft()
    while turtle.detect() == true do
        turtle.dig()
        sleep(0.5)
    end
    turtle.forward()
    turtle.select(5)
    turtle.placeDown()
    while turtle.detectUp() == true do
        turtle.digUp()
        sleep(0.5)
    end
    turtle.up()
    turtle.select(5)
    turtle.placeUp()
    turtle.place()
    turtle.down()
    turtle.place()
    turtle.turnRight()
    turtle.turnRight()
    turtle.forward()
    while turtle.detect() == true do
        turtle.dig()
        sleep(0.5)
    end
    turtle.forward()
    turtle.select(5)
    turtle.placeDown()
    while turtle.detectUp() == true do
        turtle.digUp()
        sleep(0.5)
    end
    turtle.up()
    turtle.select(5)
    turtle.placeUp()
    turtle.place()
    turtle.down()
    turtle.place()
    turtle.turnLeft()
    turtle.turnLeft()
    turtle.forward()
    turtle.turnRight()
end
turtle.turnRight()
turtle.turnRight()
length2 = length + 0
while length2 > 0 do
    turtle.forward()
    length2 = (length2 - 1)
end
length3 = (length + 1)
turtle.turnRight()
turtle.turnRight()
turtle.up()
while length3 > 0 do
    length3 = (length3 - 1)
    power = (power + 1)
    if power == 5 then
        turtle.select(2)
        turtle.placeDown()
        turtle.turnLeft()
        turtle.forward()
        turtle.select(3)
        turtle.placeDown()
        turtle.turnRight()
        turtle.turnRight()
        turtle.forward()
        turtle.forward()
        turtle.select(4)
        turtle.placeDown()
        turtle.turnLeft()
        turtle.turnLeft()
        turtle.forward()
        turtle.turnRight()
        power = 0
        turtle.forward()
    else
        turtle.select(1)
        turtle.placeDown()
        turtle.forward()
    end
end
